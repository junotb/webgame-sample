## 도메인 규칙 (2026-08 갱신 2차 — 카드 리뉴얼 반영)

> 상위 문서: `docs/design-structure.md` (세계·시스템 정의)
> 발주서: `docs/content-grid-week1.md` / 구현·검증: `docs/implementation-plan.md`
> 위 세 문서와 이 파일이 유효한 문서 전부다. 그 외 과거 사양 문서는 폐기·삭제되었으며,
> 코드 주석·커밋 메시지가 옛 문서를 인용하더라도 이 네 파일이 우선한다

### 화면

- **모바일 가로모드가 기준 화면.** 기본 스타일은 짧고 넓은 화면(≈844×390),
  데스크톱은 `min-*` 쿼리로 확장. `max-*` 축소 방향은 폐기
- 모바일은 **가로 전용.** 세로 레이아웃을 만들지 않는다 — 세로 진입 시 회전 유도 화면만
- **스크롤 금지.** 페이지·컨테이너 모두. 유일한 예외: 이벤트 진행 중 백로그(이전 텍스트 보기)
- 긴 본문은 **문단 분절 + 입력(엔터·클릭·탭) 진행.** NVL 문법 참고
  (Type-Moon — Fate/stay night, hollow ataraxia). 문서 스킨이 곧 텍스트 창 — ADV 하단 창을 만들지 말 것
- 분절은 스키마의 **문자열 배열**이다 (원소 = 문단 = 입력 한 번). `\n\n` 관례 금지, 검증기가 검사
- 한 화면에 안 들어가는 정보는 "더 보여주기"가 아니라 **장 넘김**
- 뷰포트 높이는 `dvh`, 세이프 에어리어(`env(safe-area-inset-*)`) 반영
- 백로그는 세션 로컬. 저장하지 않는다

### 상태 키

- 구역 상태는 `world:zones.*`. 치환자는 `{zone}`. `districts`/`{district}`는 폐기됨
- 구역 ID 값은 d2/d5/d7 유지. **d5 = 서편 거주구역** (확정, 특이 지형: 하수도 —
  그라우포스(회색 폭포)·세프미리(갈대 습지)·갈대밭). d2/d7 고유명사·지형 미정
- 배경은 **아케인펑크.** 지명은 사람들이 부르는 이름의 결 — 행정·지형지물의 언어.
  **주요 지명·인명은 아이슬란드어 기초 (2026-08-11 확정** — design-structure §5-4):
  지명은 투명 합성어(Gráfoss=회색+폭포), 인명은 이름 단독 호칭(부칭 문화, 성 없음)

### 노후도

- 내부 0~10, 표시는 4밴드 파생: 정상(0~2) / 삐걱임(3~5) / 이상(6~8) / 한계(9~10)
- 밴드는 저장하지 않는다. 항상 파생
- **증감은 전부 CLOSE_DAY 가중치 정산에서만.** 콘텐츠 효과에 구역 decay를 넣지 말 것
- 처리 성공 = −가중치 / 처리 실패 = 0 / 방치 = +가중치 / 자연 틱 = +1
- 미니게임 성적의 **평가 귀속은 확정**(완수/부분/실패 → 3등급).
  **노후도 가중치 귀속은 미결.** 반영하게 되더라도 정산 시점은 CLOSE_DAY 유지
- 하루치 합산 후 1회만 클램프(0~10)
- **임계 = 3밴드(이상) 진입.** 그 전에는 이상을 확인할 수 없다 —
  "임계를 넘어야 보인다"가 설계의 일부다

### 근무·배치

- 1주 = 5근무일 + 2일 휴식. 금요일 종료 시 본부의 주간 평가
- **현재 슬라이스는 1주차만 진행 후 엔딩으로 종결한다(2026-08-11 확정).**
  주간 마감 흐름: 금요일 일과 종료 → **주간 총평 장면**(상사/본부의 구두 총평 —
  이때 평가 통지서 발행·재열람 등록) → 주말 2일(택2 활동) →
  성적 분기 엔딩: Not Passed가 처리 장수의 절반 이상 → 해고 / 그 외 유임
- 4주 = 1사이클. 사이클 종료 시 해고 또는 승진 판정 — **판정 통과 후 확장분** (지금 구현하지 않음)
- 하루에 한 구역에 배치된다. 순회 개념 없음
- **승진하면 이전 구역으로 돌아가지 않는다.** 배치는 사다리가 아니라 풀 확장 —
  상위 구역은 난이도가 아니라 진실에 대한 노출도가 높은 곳

### 배타성

- **필수 이벤트(진실의 뼈대)는 관리만 잘하면 전부 볼 수 있다.**
  배타성은 선택 이벤트 쪽에만 둔다 (동료의 부탁, 이웃의 일, 꽃 한 송이)
- 필수를 배타적으로 만들지 말 것 — 최적해가 존재하는 위키 게임이 된다

### 카드 — 전면 리뉴얼

- 표면 분류 5종은 **폐기.** 현재 구조는 **일반 카드 4종** (특수 카드는 시스템 보류)
- **일반 카드 4종 = 미니게임 1:1 대응** (스키마의 명시 필드, 검증기는 이 필드를 검사):
  - 마력회로 점검 — 파이프 퍼즐
  - 구역 순찰 — 기사의 여행
  - 자재 옮기기 — 블록 퍼즐
  - 찌꺼기 소각 — 두더지 잡기 (선별형: 태울 것 / 두어야 할 것)
  - 1:1의 유일한 예외: **최초의 찌꺼기 소각 카드는 미니게임 시작 전에 조우 ENC-001이 선행된다**
    (최초 1회 한정. 조우 종료 후 미니게임 진행, 이후 소각 카드는 조우 없이 정상 미니게임)
- 얼굴 문구는 4종 안의 표현 변형. 문구는 늘려도 종류는 4개 고정
- 표면은 실제를 예고하지 않는다. 전투·상점·보상·비밀 표식을 표면 층에 두지 말 것
- **인터랙션 순서: 얼굴 → 열기 → 짧은 도입 → 미니게임 → 결과 반영 산문.**
  산문은 반드시 미니게임 **뒤.** 결과가 산문을 바꾼다 (최소 3변형: 완수/부분/실패)
- 결과 화면에서 현장 묘사(노후도 축)와 보고 문구(기억 축)는 **문단 분리**
- **특수 카드는 시스템 전체 보류. 콘텐츠에 등장시키지 않는다.**
  서사 체인·기억 관문·비밀 진행이 함께 정지 (design-structure §1-2·§8).
  보류 전 승인분(강제형 위장·1~2일 / 소멸형 비위장·사이클 존속)은 해제 시 유효
- **존속·재출현**: 일반 카드는 당일 소멸, 다음 날 같은 얼굴로 재출현 가능.
  미결 더미(backlog)는 미채택 — 못 한 일상 카드는 쌓이지 않는다
- **하루 배부 3장(무작위) · 처리 2장** — 남는 1장을 버리는 것이 트리아지.
  특수 카드는 배부 장수와 무관하게 추가 발생(보류 해제 시 유효).
  후반으로 갈수록 처리량 체감(2→1). 재발부 횟수를 UI에 표기하지 않는다
- **도시의 비밀(필수 서사)은 일과 중의 카드에서 알게 된다** (원칙 유지).
  저녁 사무소 장면·주말 활동은 관계·선택 이벤트 전용.
  서사 카드(`thread: true`·requirements 게이트)는 특수 카드 보류에 묶여 **당분간 미등장.**
  기존 서사 카드 콘텐츠(구 WO-T5~T8)는 **폐기·삭제(2026-08-11)** — 보류 해제 시 원점에서 새로 쓴다
  (`thread` 지원 코드는 스키마·생성기에 유지)
- 제목은 `TextVariant[]` — 단서가 실리는 자리. 단서 축(없음/기억/기술)은 카드 하나에
  하나만, 섞는 것은 덱 수준에서. 단서는 "여기 서사가 있다"고 말하지 않는다 (퀘스트 표식 금지)

### 평가 — 3등급으로 축소

- **Perfect / Passed / Not Passed.** 4종(완벽/양호/염려/경고)은 폐기
- **판정은 미니게임 성적 기반으로 확정**: 완수→Perfect / 부분→Passed / 실패→Not Passed.
  **주간 합산식 확정(2026-08-11) — 경계 합산식**: Not Passed가 처리 장수의 절반 이상 → 주 Not Passed /
  전 장 Perfect → 주 Perfect / 그 외 → 주 Passed.
  엔딩 분기는 같은 경계를 공유: **주 등급 Not Passed = 해고, 그 외 유임** —
  "Passed 압도적 다수" 제약 하에 정직 플레이는 유임에 안착
- 평가 통지서는 확정: 금요일 저녁 발행. 문서 ID로 **재열람에 등록**되며 완곡어 축을 탄다.
  재열람 목록에서 주 단위 구분선 역할, 아이콘 없음

### 텍스트

- 강조 표시(볼드 등)로 중요 문구를 지시하지 않는다
- 변형 축 분리: 완곡어는 `기억` 축(문서·보고서·카드 표면·보고 문구),
  악화는 `노후도` 밴드 축(현장 묘사·본문). 한 문서에 두 축을 동시에 쓰지 않는다
- **`기억` 축은 0~4 (5단계).** 관문어: 재생률(0→1) / 정상 범위(1→2) / 찌꺼기(2→3) /
  처리(3→4) / 제단(최종). 관문의 탑재 그릇은 특수 카드 보류로 **공석** —
  보류 해제 전까지 기억은 0 고정
- **비밀 접근의 축은 "소각 → 마력" 현상의 규모 반복이다** (개인: 물증 미정 — 구 가로등안
  폐기(2026-08-11) / 구역: 재생률 / 도시: 축제).
  수렴은 순차 3단계 — 소리 → 문서 → 축제 → 3사이클 제단 돌입("존재" 조우)
- **4초 리듬은 폐기 (2026-08-11 확정).** `saw_rhythm` 플래그와 "4초" 수치 서술을
  콘텐츠에서 제거했다. "지나치게 고른 간격"류의 분위기 서술만 남기며,
  수치·플래그로 되살리지 않는다
- **렌더링된 문장을 저장하지 않는다.** 저장 대상은 문서 ID와 상태값뿐
- 과거 문서도 **현재** `기억` 값으로 재렌더링한다 (그때 값으로 렌더링 금지, 테스트로 고정)

### 마법

- **화염술 축적 경로는 두 가지로 제약**: ① 조직 교육 수강 ② 찌꺼기 소각 카드
- **빙결술은 어머니의 유산 경로로만**: 마법서 자기주도학습(주말 슬롯) → "돌아온 자" 경험치 이벤트 → 유물.
  **기록은 정식 스킬로(2026-08-11 확정·구현됨)**: `frost`가 skills/skillXp에 있다.
  등급 0 = 미습득 — 능력치 패널은 0등급 스킬을 숨긴다 (존재 자체가 스포일러)
- 진눈깨비 효과 = 마력 관련 버프 + 화염술 경험치 (2경로 제약과의 관계 확인 필요 — design-structure §8)
- 제단 돌입 해금 = 마지막 비밀 인지 + 진눈깨비 서술 전환 판정을 받은 상태
- 종반 분기 (design-structure §4-0): 축제 주 [화염 최고] 특수 카드 선택 → 소각자 확정.
  돌입 조우에서 "막는다"(화염 최고 → 소각자 / 미달 → 데드) 또는
  "도망친다"(극지 불시착 → 빙결 최고 시 빙결 엔딩 가능)

### 판정 정보

- 선택지가 있는 산문(조우 등)에는 판정 기술·스탯명과 성공률(%)을 표기한다.
  일반 카드의 판정은 미니게임으로 대체되었으므로 해당 없음. 특수 카드는 보류 중이므로 대상 없음
- 비공개 대상은 노후도 **수치** 하나뿐. 밴드는 보인다

### 아이콘

- 표면 층에만 존재. 실제 층을 예고하지 않는다
- 상태에 반응해 변하지 않는다
- **어휘 크기 = 일반 카드 4종과 일대일.** 대응 대상은 카드 종류 필드이지 얼굴 문구가 아니다
- 조우에는 아이콘을 두지 않는다. 평가 통지서도 아이콘 없음 (어휘 밖)

### 조우

- `core/encounter.ts` 독립 리듀서. GameState 밖 로컬 상태
- 종료 시 outcome별 효과만 `RESOLVE_ENCOUNTER`로 반입. 조우 안에서 세계 상태를 직접 건드리지 말 것
- 조우 상태는 저장되지 않는다 (슬라이스 한정 단순화)
- **ENC-001 진입 확정**: 최초의 찌꺼기 소각 카드를 열 때, **미니게임 시작 전에 선행** —
  찌꺼기는 "존재"에서 떨어져 나온 것으로 짐작되는 살아있는 잔재이므로,
  첫 소각 업무가 곧 첫 대면이다. 조우 종료 후 미니게임이 이어진다.
  **카드 성적 귀속은 이때도 미니게임 결과가 맡는다(확정)** — 조우 outcome은 성적에 관여하지 않는다
- 질감 규정(의식·절차 언어·감정어 금지)은 **폐기(2026-08-11)** — 조우 문장에 별도 문체 제약 없음
- 오버레이 셸은 **공통 셸**(조우·다일·탐사 공용)로 구현 — implementation-plan 세션 ④

### 세이브

- `{schema, state}` 봉투. 불일치 시 새 게임. 현재 SAVE_SCHEMA 9 (원본은 `app/save.ts` — 이 숫자가 낡았으면 코드가 맞다)
- 스키마 변경 시 "렌더 문장 미저장" 제약을 매번 재확인할 것
- 카드 리뉴얼로 스키마 변경 시(카드 종류 필드·존속 필드·미니게임 결과) SAVE_SCHEMA 증가

### 고도

- 실패 상태가 아니다. 게임오버·부활 조건과 무관
- 주간 루프 UI에 표기하지 않는다. 내부 수치는 에피소드 스케일 장치로만 사용
