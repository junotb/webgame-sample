# CLAUDE.md — 신규 프로젝트 (가제 미정)

상용 1인 개발 텍스트 기반 브라우저 게임. 모든 판단의 첫 번째 필터는 **"1인이 지속 가능한가"**.

**한 줄 콘셉트: 무너지는 것을 매일 고치는 사람의 이야기.**
활기찬 아케인펑크 공중 도시에서 마력 인프라를 정비하는 신입 조직원. 밝은 문체 아래 어둠이 배어 나온다.

> 이 문서는 Claude Project 가이드라인의 사본이다. **설계 결정의 원본은 Claude Project에 있으며,
> 설계 변경은 그쪽에서 결정 후 이 문서에 반영한다** (단방향 동기화).
> 코드 세부 규칙은 이 문서에만 쌓여도 된다.
> ※ 구 프로젝트 "부서진 왕국의 연대기"(그리드 크롤러, 6스탯, Roblox)는 전부 폐기됨 — 절대 끌어오지 않는다.

---

## 1. 코드 규칙 (최우선)

### 디렉토리와 의존 방향

```
리포루트/
├── CLAUDE.md        이 문서
├── docs/            phase 스펙, icon-vocabulary, CREDITS — 설계·규격 문서는 전부 루트 docs
└── web/             Next.js 앱
    ├── core/        게임 로직. 순수 TS만 — React/Next/브라우저 API/lib/app import 금지 (ESLint로 강제)
    ├── app/         Next.js 라우팅 + UI. core를 소비만 한다
    ├── lib/         backend.ts (백엔드 인터페이스), 콘텐츠 로더, IndexedDB
    ├── content/     JSON 콘텐츠 번들. 코드에서 직접 import 금지 — 매니페스트 경유 비동기 로드만
    ├── assets/icons/  game-icons SVG (SVGR 경유, currentColor 색 치환)
    └── public/maps/   도시 지도 이미지 등 색 치환 불필요한 정적 파일
```

- **풀스택 계획**: 서버 워크스페이스는 Phase 2 진입 시점에 추가한다 — 지금 모노레포
  워크스페이스 설정을 하지 않는다. 그때 `web/core/` → `packages/core/` 승격 후
  서버가 core를 패키지로 소비한다 (core 무의존 원칙이 이 승격 비용을 0에 가깝게 유지한다)

- **core/는 테스트 없이 수정하지 않는다.** 구현 전 테스트 먼저 (경계값 포함)
- **작업은 작은 단위로.** 한 세션 = 한 모듈 + 테스트. 여러 시스템을 한 번에 구현하지 않는다
- 리듀서 계약: `(GameState, Action, ContentBundle) → { state, log }`. 순수 함수, 원본 불변
- PRNG는 seedable (같은 시드 = 같은 결과, 테스트로 보장)
- 리듀서는 **완성된 것만 받는다**: 경로 치환(`{district}`)·난이도 보정(difficultyBonus)은
  지시서 생성 시점에 굽는다. 리듀서에 치환/보정 로직을 넣지 않는다

### 불변 규칙 (테마가 코드에 새겨진 지점)

- **`self.memory`(기억)는 비가역.** 감소시키는 effect는
  ① 콘텐츠 빌드 검증에서 거부하고 ② effect 적용기에서 런타임 throw한다 (이중 방어)
- 미지 치환자·깨진 참조는 조용한 no-op 금지 — 즉시 throw (fail-fast)

### 아키텍처 확정 (D1~D4)

- **D1 단계 이행형**: Phase 0~1은 클라이언트 권위 + 정적 번들 (서버 비용 0원).
  Phase 2+에서 계정·구매·유료 콘텐츠 배포만 서버 추가. 액션 게이트는 동기화 시점 검증 수준
- **D2**: Next.js `output: 'export'` 정적 사이트. 캔버스/게임엔진 불사용 — HTML/CSS 렌더링
- **D3**: Supabase는 "관리형 Postgres + 인증(매직링크)"으로만.
  모든 백엔드 접근은 `lib/backend.ts` 인터페이스 뒤에 — 화면 코드에 supabase 직접 호출 금지.
  RLS 복잡 정책·Edge Functions에 로직 금지
- **D4**: 콘텐츠 = TS 스키마 + JSON, 빌드 시 검증(참조 무결성·조건식·치환자·기억 감소 거부).
  유료 콘텐츠의 "서버 배포" = 인증 걸린 JSON 서빙일 뿐, 서버는 게임 로직을 모른다
- 스포일러 보호선: 무료 공개분만 정적 번들 허용. 진실 심부·유료 에피소드는 번들에 넣지 않는다

### scope creep — 발견 즉시 지적할 것

실시간 기능(SignalR류)·멀티플레이·랭킹·자체 대시보드·"꺼지면 안 되는 서버"를 만드는 모든 제안.
합의 범위 밖 제안을 할 때는 반드시 "합의 범위 밖 제안"임을 명시한다.

---

## 2. 게임 시스템 규격

### 3시트 상태 (분류 기준 = 수명)

| 시트    | 수명            | 내용                                                                                   |
| ------- | --------------- | -------------------------------------------------------------------------------------- |
| account | 서비스          | 여력(액션: 상한 20/서포터 40, 10분당 1 회복 — Phase 1에서 활성화), 구독, 소유 에피소드 |
| self    | 에피소드를 넘음 | 주 스탯 4종, 전문 기술 6분과, **기억**(비가역), 직위                                   |
| world   | 에피소드        | 구역 노후도, 고도, 항로, NPC 신뢰/유대, 메나스, 플래그                                 |

에피소드 리셋 = `world` 폐기. 세이브도 같은 3분할.

### 판정 수식

- **Broad** (주 스탯 4종: 정비/진단/절차/담력, 0~100): `p = clamp(0.6 × stat / difficulty, 0.05, 1.0)`
- **Narrow** (전문 기술, 0~7): `p = clamp(0.5 + (skill − difficulty) × 0.1, 0.1, 1.0)`

### 메나스 (0~8, 도달 시 강제 이동 — 게임오버 아님)

| 메나스 | 의미                      | 넘치면                                                  |
| ------ | ------------------------- | ------------------------------------------------------- |
| 피로   | 몸의 한계                 | 의무실                                                  |
| 주목   | 조직의 시선 (캐는 비용)   | 감사실 소환                                             |
| 동요   | 마음의 흔들림 (아는 비용) | "적응 지원 프로그램" — 동요는 내려가도 기억은 안 내려감 |

### 전문 기술 학제 (Narrow) — 기초 3 + 응용 2 + 금기 1

| 분과       | 역할                  | 공식 명칭                           | 개방         |
| ---------- | --------------------- | ----------------------------------- | ------------ |
| 각인학     | 쓰기 → 마법 성공률    | 회로각인학                          | Ep1          |
| 감류학     | 읽기 → 마법 효과/강도 | 동일                                | Ep1          |
| 공명학     | 듣기 → 사전 인지      | "음향 점검"                         | Ep2          |
| 화염학     | 태우기 → 조우 대응    | "연소 공학"                         | Ep3          |
| 빙결학     | 재우기 → 비살상 대응  | **무명 (동력이 안 나와 안 가르침)** | 추운 지역 Ep |
| 증식육체론 | 이해 → 도감 게이트    | **무명 (학문이 없는 것으로 됨)**    | 최후반       |

화염 마법은 각인/감류 등급 조합으로 개방. 조우의 핵심 선택: **태울 것인가 vs 재울 것인가** (결말 축).

### 핵심 시스템 장치

- **완곡어 시스템**: 같은 텍스트가 `self.memory`에 따라 다르게 렌더링 — `TextVariant[]`,
  조건 첫 매치 우선, 무조건 변형이 기본값(맨 뒤). 이 게임의 심장
- **도시 지도**: 이미지 1장 + SVG 폴리곤 클릭 오버레이(이미지와 분리) + game-icons 마커
  (메인=스토리렛/서브=지시서/일상). 점진 공개는 `Condition`으로.
  `MapVariant { if, src }` = TextVariant의 이미지 동형 — **모든 변형은 레이아웃 동일**.
  노후도 등 일상 변화는 이미지가 아니라 CSS 오버레이/필터로
- **지시서 생성기**: 구역 노후도 × 템플릿(minDecay) → 일일 콘텐츠 자동 생산.
  방치 구역은 노후도 상승 → 다음 날 난이도에 반영 (트리아지의 대가)

---

## 3. 콘텐츠 작성 규격 (에이전트 필독)

- 스키마(`web/core/schema.ts`)가 작업 규격이다. 빌드 검증을 통과해야 콘텐츠다
- **아이콘은 `docs/icon-vocabulary.md`의 의미 ID로만 참조한다.** 파일명 직접 언급 금지.
  새 아이콘이 필요하면 등록부에 의미 ID를 먼저 등록 (파일 선정은 나중이어도 됨)
- 치환자(`{district}`)는 **지시서 템플릿에만** 허용 (TemplateEffectPath). 스토리렛엔 금지
- `self.memory` 감소 effect 금지 (검증기가 거부)
- **톤 규칙 — 이 게임의 상품**:
  - 표면은 평화롭고 활기찬 문장. 어둠은 직접 말하지 않고 **행간에 배어 나오게** 한다
  - 조직 문서는 완곡어로: "연료 재생률 변동", "설비 이상", "과증식 조직 절제", "적응 지원 프로그램"
  - memory 조건 변형에서만 완곡어가 벗겨진다 — 벗겨진 쪽도 절규하지 않는다. 조용할수록 무섭다
  - 기준 예시: `content/ep1-slice`의 WO-T3(간헐 명멸)과 EV-001(사무소의 멈춤)이 톤의 기준점
- 몬스터는 문서상 생물이 아니다 — 항상 "설비 이상"으로 표기 (도감·보고서·지시서 전부)

---

## 4. 세계 설정 요약 (스포일러 포함 — 콘텐츠 작성용 진실)

- 무대: **이동하는 공중 도시.** 동력망이 도시를 띄운다 — 붕괴 = 추락. 고도가 남은 시간
- **진실 (영구기관)**: 어떤 존재의 무한히 증식하는 육체를 대마법사의 고위 화염 마법으로 태워
  동력으로 쓴다. 오랜 나태/타락/안일로 그 존재가 육체를 수복해 사건이 시작된다.
  그 존재의 분노와 자유를 향한 갈망 = "빚"의 청산. 몬스터는 그 존재의 파생
- 구조적 아이러니: ① 축제가 밝을수록 종말이 가까워진다 ② 수리가 곧 가담이다
- 침묵의 유지: 공식 완곡어 + 과잉 축제
- 인물 구조: **"나"(플레이어)** = 신입, 진실 축은 '알아가기'(기억).
  **에피소드 1 주인공 "돌아온 자"** = 진실을 알고 잊으려 떠났다 복직한 동료, 특정 이벤트의 계시자.
  드라마틱 아이러니는 두 사람 사이 — 나는 읽고 넘어가고, 그는 그 단어 앞에서 멈춘다
- 종말관: 막을 수 없다. 이야기는 "어떻게 버티고, 무엇을 지키고, 무엇을 놓아줄 것인가"
- 결말 후보: 추운 지역에서 사건 → 도시 추락 → 그 존재가 호수에서 만년설이 되어 잠듦
- 매크로 아크: 에피소드 = 무대와 "멸망의 씨앗"이 바뀌는 단위. 월드가 파멸해도
  캐릭터는 기억을 지니고 다음 에피소드로 — 플레이어는 점차 "기억하는 자"가 되어간다

---

## 5. 단계별 경로 (순서 건너뛰기 금지)

- **Phase 0** (현재): 수직 슬라이스 — 하루 루프 1주기. 서버 0원. 스펙: `docs/phase0-spec.md`
- **Phase 1**: 정적 호스팅 무료 공개. 지표는 D7 재방문 하나. 액션 게이트 활성화
- **Phase 2**: 계정 + 클라우드 동기화 (Supabase)
- **Phase 3**: 수익화 — MoR(Paddle/Lemon Squeezy) + 본편 언락. 직전에 GRAC 등급분류·사업자등록 해결
- **Phase 4**: 월간 에피소드/분과 판매 리듬

수익 원칙: 코어는 1회 구매, 구독은 옵션(여력 상한 확대), Pay-to-win 없음.
운영 원칙: 운영 비용(시간+돈) 0 수렴. 월 1회 정기 패치 + 핫픽스만 — 주간 패치 금지.
