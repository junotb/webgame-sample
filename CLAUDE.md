# 작업 규칙 — 코드 불변식

> **이 파일에는 코드를 바꾸는 것만 쓴다.** 근거·서사·세계관·주제는 쓰지 않는다.
> 판별: *이 줄이 없으면 코드를 틀리게 쓰는가?* 아니면 `docs/`로 보낸다.
>
> 요약본이 아니라 **다른 층의 문서다.** 여기 없는 것은 누락이 아니라 의도적 배제이며,
> 그런 질문은 아래 라우팅에 따라 원본을 열어서 답한다.
> 원본과 충돌하면 원본이 옳다 — 이 파일의 해당 줄을 고친다.

## 문서 라우팅

| 물음 | 열 문서 |
| --- | --- |
| 세계·시스템의 정의, 왜 이런 규칙인가, 엔딩·비밀·명명 | `docs/design-structure.md` |
| 산문을 어떻게 쓰는가 (규격·가드레일·지명 표기) | `docs/design-structure.md` §12 |
| 1주차 범위, 분량 상한 | `docs/design-structure.md` §11 |
| 카드·장면의 실제 목록 | `web/content/content.ep1-slice.json` (정본) |
| 남은 미결, 이후 과제 | `docs/design-structure.md` §9 |
| 위 어디에도 없음 | **미결이다. 추측해서 채우지 말고 물어볼 것** |

과거 사양 문서는 폐기·삭제되었다. `implementation-plan.md`(판정 게이트 폐기)와
`content-grid-week1.md`(1주차 콘텐츠 작성 완료)도 2026-08-13 함께 폐기됐고,
살아 있던 내용은 모두 design-structure(§9·§11·§12)로 흡수됐다.
코드 주석·커밋 메시지가 옛 문서를 인용하더라도 **이 파일과 `docs/design-structure.md`**
두 개가 전부다.

---

## 화면

- **모바일 가로모드 기준** (≈844×390). 데스크톱은 `min-*` 확장. `max-*` 축소 방향 폐기
- 모바일 **가로 전용.** 세로 레이아웃 없음 — 세로 진입 시 회전 유도 화면만
- **스크롤 금지** (페이지·컨테이너 모두). 예외: 이벤트 진행 중 백로그
- 긴 본문은 **문단 분절 + 입력 진행** (NVL 문법). ADV 하단 창을 만들지 말 것
- 분절 = 스키마의 **문자열 배열** (원소 = 문단 = 입력 1회). `\n\n` 금지, 검증기가 검사
- 한 화면에 안 들어가면 "더 보여주기"가 아니라 **장 넘김**
- 뷰포트 높이 `dvh`, `env(safe-area-inset-*)` 반영
- 백로그는 세션 로컬. 저장하지 않는다

## 상태 키

- 구역 상태 `world:zones.*`, 치환자 `{zone}`. `districts`/`{district}` 폐기
- 구역 ID: d2 / d5 / d7. d5 = 서편 거주구역
- 노후도 내부 0~10, 표시는 4밴드 파생: 정상(0~2) / 삐걱임(3~5) / 이상(6~8) / 한계(9~10)
- **밴드는 저장하지 않는다. 항상 파생**
- `기억` 0~4 (5단계). **보류 해제 전까지 0 고정**
- 관문어: 재생률(0→1) / 정상 범위(1→2) / 불순물(2→3) / 처리(3→4) / 제단(최종)
- **기술 5종.** 표기 순서 = 언어 3종 → 술법 2종:
  `flowsense` 감류학(듣기) / `incantation` 영창술(말하기) / `inscription` 각인학(쓰기) /
  `flame` 화염술 / `frost` 빙결술
- 시작 등급은 4종 1, **`frost`만 0. 등급 0 = 미습득이며 능력치 패널이 숨긴다**
- 등급은 파생이다 — 콘텐츠 효과는 `self.skillXp.*`에만 쓴다 (`self.skills.*` 쓰기 경로 없음)

## 노후도 정산

- **증감은 전부 `CLOSE_DAY` 가중치 정산에서만.** 콘텐츠 효과에 구역 decay를 넣지 말 것
- 처리 성공 = −가중치 / 처리 실패 = 0 / 방치 = +가중치 / 자연 틱 = +1
- 하루치 합산 후 1회만 클램프(0~10)
- 미니게임 성적의 **평가 귀속은 확정**, **노후도 가중치 귀속은 미결.**
  반영하더라도 정산 시점은 `CLOSE_DAY` 유지
- 임계 = 3밴드(이상) 진입. 그 전에는 이상을 확인할 수 없다

## 근무 루프

- 1주 = 5근무일 + 2일 휴식. **현재 슬라이스는 1주차만 진행 후 엔딩으로 종결**
- 주간 마감: 금요일 일과 종료 → 주간 총평 장면(통지서 발행·재열람 등록) →
  주말 2일(택2) → 성적 분기 엔딩
- **해고는 총평 장면 없이 금요일 정산에서 곧장 엔딩.** 총평·주말은 유임자만 거친다
  (통지서 발행은 동일)
- **저녁(event) 단계는 조건 맞는 장면이 있을 때만 선다.** 빈 저녁은 곧장 일일 정산
- 주말 슬롯 = 2일 × 택2. 빙결 학습과 인물 3인이 같은 풀에서 경쟁
- 하루에 한 구역 배치. 순회 없음
- 4주 사이클·승진·해고 판정식은 **1주차 완주 후 확장분 — 지금 구현하지 않는다**

## 카드

- **일반 카드 4종. 특수 카드는 시스템 전체 보류** (콘텐츠에 등장시키지 않는다).
  서사 체인·기억 관문·비밀 진행이 함께 정지. `thread` 지원 코드는 스키마·생성기에 유지
- **4종 = 미니게임 1:1** (스키마 명시 필드, 검증기가 검사):

  | 카드 | 미니게임 |
  | --- | --- |
  | 마력회로 점검 | 파이프 퍼즐 |
  | 구역 순찰 | 한붓 순찰 (시작→끝, 전 칸 1회 통과. 게임 id `knight`는 잔재) |
  | 자재 옮기기 | 블록 퍼즐 |
  | 불순물 소각 | 두더지 잡기 (선별형: 태울 것 / 두어야 할 것) |

- **1:1의 유일한 예외: 최초의 불순물 소각 카드는 미니게임 전에 ENC-001이 선행**
  (최초 1회 한정)
- 얼굴 문구는 4종 안의 표현 변형. 문구는 늘려도 종류는 4개 고정
- **표면은 실제를 예고하지 않는다.** 전투·상점·보상·비밀 표식을 표면 층에 두지 말 것
- **인터랙션 순서: 얼굴 → 열기 → 짧은 도입 → 미니게임 → 결과 반영 산문.**
  산문은 반드시 미니게임 뒤. 결과가 산문을 바꾼다 (최소 3변형: 완수/부분/실패)
- 결과 화면에서 현장 묘사(노후도 축)와 보고 문구(기억 축)는 **문단 분리**
- 존속: 당일 소멸, 다음 날 같은 얼굴로 재출현 가능. **미결 더미(backlog) 미채택**
- **배부 3장(무작위) · 처리 2장.** 하루 3장은 종류(kind)가 겹치지 않는다 —
  적격 풀에 서로 다른 종류가 모자랄 때만 중복 허용
- **ENC-001 발생 전까지 소각 카드가 매일 1장 포함된다** (최초 대면 보장)
- 4종 전부 주 3회 이상 배부. 기준은 배부(주 15장)이지 처리(주 10장)가 아니다
- 재발부 횟수를 UI에 표기하지 않는다
- 제목은 `TextVariant[]`. 단서 축(없음/기억/기술)은 카드 하나에 하나만.
  퀘스트 표식 금지

## 평가

- **Perfect / Passed / Not Passed** (4종 폐기)
- 장별: 완수→Perfect / 부분→Passed / 실패→Not Passed
- **주간 경계 합산식**: Not Passed가 처리 장수의 절반 이상 → 주 Not Passed /
  전 장 Perfect → 주 Perfect / 그 외 → 주 Passed
- 엔딩 분기는 같은 경계 공유: **주 Not Passed = 해고, 그 외 유임**
- `weekTally` 합산은 금요일 시점 값 유지 (테스트로 고정)
- 통지서: 금요일 저녁 발행, 문서 ID로 재열람 등록, 완곡어 축을 탄다. 아이콘 없음

## 텍스트 렌더링

- **렌더링된 문장을 저장하지 않는다.** 저장 대상은 문서 ID와 상태값뿐
- **과거 문서도 현재 `기억` 값으로 재렌더링한다** (그때 값 렌더링 금지, 테스트로 고정)
- 변형 축 분리: 완곡어 = `기억` 축(문서·보고서·카드 표면·보고 문구) /
  악화 = `노후도` 밴드 축(현장 묘사·본문). **한 문서에 두 축을 동시에 쓰지 않는다**
- **강조 표시(볼드 등)로 중요 문구를 지시하지 않는다**
- **지명은 치환자 대상이 아니다.** 오버레이 타이틀·고유명사는 `기억`·`노후도`
  어느 축으로도 변형되지 않는다 — 앵커다. `renderArchiveEntry`·완곡어 치환 경로에서
  섞이지 않도록 **테스트로 고정할 것**
- `saw_rhythm` 플래그와 "4초" 수치 서술 금지 (되살리지 말 것)

## 아이콘

- 표면 층에만 존재. 실제 층을 예고하지 않는다
- **상태에 반응해 변하지 않는다**
- **어휘 크기 = 일반 카드 4종과 1:1.** 대응 대상은 카드 종류 필드이지 얼굴 문구가 아니다
- 조우·평가 통지서에는 아이콘을 두지 않는다 (어휘 밖)

## 조우

- `core/encounter.ts` 독립 리듀서. GameState 밖 로컬 상태.
  종료 시 outcome별 효과만 `RESOLVE_ENCOUNTER`로 반입 —
  **조우 안에서 세계 상태를 직접 건드리지 말 것**
- 조우 상태는 저장되지 않는다 (슬라이스 한정 단순화)
- **ENC-001**: 최초 불순물 소각 카드를 열 때 미니게임 **전에** 선행.
  설명형 (`EncounterDef.briefing`, outcome `briefed`) — 행동·턴·판정 없음.
  효과는 동요 +1. **성적 귀속은 미니게임 결과가 맡는다** (조우 outcome은 성적에 무관)
- 4행동×N턴 다단 구조는 폐기가 아니라 **향후 조우의 그릇**으로 코드에 유지
- 오버레이 셸은 **공통 셸** (조우·다일·탐사 공용)

## 판정 정보 표기

- 선택지가 있는 산문에는 판정 기술·스탯명과 성공률(%)을 표기한다.
  일반 카드는 미니게임으로 대체되어 해당 없음. 특수 카드는 보류 중이라 대상 없음
- **비공개 대상은 노후도 수치 하나뿐.** 밴드는 보인다

## 세이브

- `{schema, state}` 봉투. 불일치 시 새 게임.
  현재 SAVE_SCHEMA 13 — **원본은 `app/save.ts`이며, 이 숫자가 낡았으면 코드가 맞다**
- 스키마 변경 시 "렌더 문장 미저장" 제약을 매번 재확인할 것

## 고도

- 실패 상태가 아니다. 게임오버·부활 조건과 무관
- 주간 루프 UI에 표기하지 않는다. 내부 수치는 에피소드 스케일 장치로만 사용

---

## 규칙 추가·수정 시

- **근거를 쓰고 싶어지면 그 줄은 `docs/`로 간다.** 여기에는 결론만 남긴다
- 원본(`docs/`) 갱신이 먼저다. 이 파일은 그 뒤에 단방향으로 따라간다
- 여기에 없는 것을 추측으로 채우지 않는다 — 라우팅 표의 마지막 행을 따른다
