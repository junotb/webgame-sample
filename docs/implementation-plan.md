# 구현·검증 계획

> 배치: `docs/implementation-plan.md`
> 성격: 판정 기준과 개발 순서. 주차 문서와 분리해 두어 2주차 이후에도 이어진다
> 상위 문서: `docs/design-structure.md`
> 형제 문서: `docs/content-grid-week1.md`

---

## 1. 현재 상태

- **첫 슬라이스(v1)는 기술적으로 완주했으나 플레이 판정 3항목 모두 실패.**
  하루 30초 통과 / 트리아지 무고민 / 완곡어 미인지.
  원인은 공통 — **"이 선택이 어떤 의미인지 알기 어려웠다"**
- 재작업 중 카드 구조를 전면 리뉴얼(미니게임 도입). Phase 1 스펙 수립은 계속 보류
- 코드베이스: `web/` 아래 Next.js. core에 schema/checks/bind(+테스트).
  bind는 `TemplateEffectPath` 분리·미지 치환자 throw 등 v1 스키마에서 진화

---

## 2. 판정 목표 4

1. **놀라는가** — 경고창 없이, 문장 한 자리의 차이로
2. **읽는가** — 미니게임 직후에도 산문을 읽는가
3. **알아채는가** — 완곡어의 액면과 실제의 간격을
4. **금요일이 무서운가**

**1~2 실패 시 4주 사이클로 넘어가지 않는다.**
목표 3은 특수 카드 보류로 이번 게이트에서 **제외** — 보류 해제 후 재측정한다.

### 새 구조에서 각 목표의 위험 지점

| 목표 | 새 위험                                                              |
| ---- | -------------------------------------------------------------------- |
| 1    | 미니게임이 놀람의 자리를 선점할 수 있다                              |
| 2    | **가장 큰 위험.** 30초 퍼즐 직후 각성 상태에서 산문을 건너뛸 수 있다 |
| 3    | 미니게임에 인지 자원을 다 쓰면 완곡어가 안 보인다                    |
| 4    | 평가 판정 기준이 미결 — 기준 확정 후 위험 재평가                     |

**목표 2가 이번 판정의 실질적 관문이다.** 여기서 실패하면 미니게임 도입 자체를 재검토한다.
대비책은 산문 후치와 결과 반영 분기이며, 플레이테스트 시 이 항목을 최우선 관찰한다.

**목표 3은 이번 판정에서 측정 불가.** 특수 카드 보류로 기억 관문이 없어 기억이 0에 고정된다.
이번에는 기억 0 문장의 반복 노출(기준선)만 확인하고, 관문 통과 검증은 보류 해제 후로 연기한다.

### v1 실패의 반대 실패

미니게임 + 특수 카드 존속이 얹히면서 **"하루가 지겹다"**는 반대 방향 실패가 가능해졌다.
하루 처리 2장 = 3~4분, 주 15~20분을 상한으로 본다. 넘으면 카드 수를 줄인다.

---

## 3. 검증 계획

| 회차  | 내용                                       | 확인 대상     |
| ----- | ------------------------------------------ | ------------- |
| 1회차 | 정직 플레이 (5근무일 완주 → 유임 엔딩)     | 판정 목표 1~4 |
| 2회차 | 평가를 일부러 무너뜨림 (같은 1주차 안에서) | 해고 엔딩     |

- 1회차 전 확인: 미니게임 3연타 후 산문 체류 시간, 재열람 진입 여부
- **① 단독 리뷰 시 "하루 30초" 실패 원인을 재열람 유무와 함께 본다** —
  하루가 30초로 끝난 이유 중 하나가 되돌아볼 곳이 없어서일 수 있다
- ①에서는 `기억`이 0이므로 다시 열어도 같은 문장이 나온다. 그것이 정상이며,
  그 "똑같음"이 이후 변화의 기준선이 된다

### 계측 (콘솔·스토리지 한정)

- 일별 소요 시간
- 재열람 진입 횟수 · 문서별 재열람 수
- 카드 체류 시간 (미니게임 시간과 산문 시간을 **분리 측정**)

산문 체류 시간이 미니게임 시간보다 짧으면 목표 2 실패로 본다.

---

## 4. 세션 분할

| #   | 세션               | 내용                                                       |
| --- | ------------------ | ---------------------------------------------------------- |
| ①   | 시간·평가 코어     | 5일 루프, 평가 3종, 재열람 껍데기(서랍·목록·열기)          |
| ②   | **미니게임 셸**    | 4종 공통 인터페이스, 결과 → 산문 분기                      |
| ③   | 카드 레이어        | 얼굴 4종, 존속·재출현 (위장 특수는 보류로 제외)            |
| ④   | 조우 리듀서        | 오버레이 **공통 셸** (조우·다일·탐사 공용 — 여기서 공통화) |
| ⑤   | 완곡어 렌더링 확장 | 재열람 재렌더 함수 `(문서 ID, 기억) → 문장`                |

- **①이 나머지의 전제 — 단독 실행 후 리뷰**
- **② 신설이 이번 변경의 핵심 추가분이며, ③이 ②에 의존한다**
- ⑤가 `(문서 ID, 기억) → 문장` 함수를 채우면 재열람 서랍이 저절로 살아난다
- **①~⑤ 전 세션 구현 완료 (2026-08-11).** ⑤는 `core/rerender.ts`의 `renderArchiveEntry` —
  "과거 문서도 현재 기억으로"를 테스트로 고정. 기억 0 고정이라 현재 출력은 기준선이다

---

## 5. 코드 작업 목록

### 필요 (1주차 범위)

| 항목                                                                                | 규모                                                                                            |
| ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 미니게임 4종 구현                                                                   | **최대 항목.** 파이프 / 기사의 여행 / 블록 / 선별 두더지                                        |
| 카드 → 미니게임 → 산문 파이프라인                                                   | 중                                                                                              |
| 미니게임 결과 → 산문 변형 분기                                                      | 소                                                                                              |
| 미니게임 성적 → 평가 귀속                                                           | **확정** — 완수/부분/실패 → 3등급                                                               |
| 주간 합산 → 엔딩 분기                                                               | 소 — Not Passed가 처리의 절반 이상 → 해고, 그 외 유임                                           |
| 엔딩 화면 (유임/해고 맺음 산문 → 종료)                                              | 소                                                                                              |
| 미니게임 성적 → 노후도 가중치 귀속                                                  | **미결 — 결정 후 착수**                                                                         |
| 평가 3종 축소                                                                       | 소                                                                                              |
| 평가 통지서 문서 ID 재열람 등록                                                     | 소                                                                                              |
| ~~특수 카드 존속 기간 필드~~                                                        | 보류 (특수 카드 시스템 보류)                                                                    |
| 일상 카드 재출현 로직                                                               | 소                                                                                              |
| 배부 추첨 — 무작위 3장 + 보장 규칙(ENC-001 발생 전 소각 매일 1장 · 4종 주 3회 노출) | 소 (고정 배치표 폐기 — 2026-08-11)                                                              |
| ~~주간 총평 장면 (금요일 일과 종료 직후, 등급별 변형 — 통지서 발행·등록 포함)~~     | **완료(2026-08-11)** — `debrief` 단계, 통지서는 `{kind:'notice'}`로 재열람 등록                 |
| ~~주말 2일 택2 슬롯 (빙결 학습 · 인물 3인) + 엔딩 진입을 주말 뒤로 이동~~           | **완료(2026-08-11)** — `weekend` 단계 + `frost` 정식 스킬(SAVE_SCHEMA 12), 엔딩은 4슬롯 뒤 확정 |
| 최초 소각 카드 → ENC-001 선행 삽입                                                  | 소 (최초 1회 플래그 + 열기 시 조우 → 미니게임 체인)                                             |

### 불필요

- 서사 체인 (특수 카드 보류에 묶임 — 재개 시에도 플래그 requirements로 해결, 코드 불필요)
- 미결 더미 UI (소멸 방식 채택)
- 특수 전용 아이콘·얼굴 (특수 자체가 보류. 재개 시에도 만들지 않는 것이 승인분)

### 구현 제약 (불변)

- **렌더링된 문장을 저장하지 않는다.** 세이브 데이터에 완성된 문장이 들어가면
  재렌더가 슬그머니 보관본이 된다. 저장 대상은 문서 ID와 상태값뿐이다
- **과거 지시서에도 현재 `기억`을 적용한다.** "그때 값으로 렌더링"이 자연스러운 구현이므로
  명시하지 않으면 나중에 그쪽으로 고쳐진다. **테스트로 고정할 것**
- 조우는 하루 루프와 분리된 독립 리듀서로 유지 (향후 전환 비용 보호)

---

## 6. 착수 전 미결

0. ~~1주차 이후 진행~~ **확정(2026-08-11)**: 4주 사이클로 확장하지 않고
   **1주차만 진행 후 엔딩으로 종결**. 금요일 평가 통지서 → 성적 분기 엔딩(유임/해고).
   주간 합산식도 이에 맞춰 확정: **Not Passed가 처리 장수의 절반 이상 → 해고, 그 외 유임**
   ("Passed 압도적 다수" 제약 충족 — 정직 플레이는 유임에 안착).
   4주 사이클·승진/해고 판정식은 판정 통과 후 과제로 이동
1. ~~하루 배부 장수~~ **확정(2026-08-11)**: 배부 3장(무작위) · 처리 2장. 특수 카드는 배부 장수 밖
2. ~~미니게임 시작 종수~~ **확정(2026-08-11)**: 4종 전부 만들고 시작
3. ~~평가 판정 기준~~ **확정(2026-08-11)**: 미니게임 성적 기반 (완수→Perfect / 부분→Passed /
   실패→Not Passed). 주간 합산식도 확정 — **경계 합산식**: Not Passed ≥ 처리 절반 → 주 Not Passed /
   전 장 Perfect → 주 Perfect / 그 외 → 주 Passed. 주 Not Passed = 해고 (엔딩과 경계 공유)
4. ~~조우 진입 경로~~ **확정(2026-08-11)**: 최초의 불순물 소각 카드를 열 때 ENC-001 발생
   (미니게임 시작 전 선행, 조우 종료 후 미니게임 진행. 최초 1회 한정) — 1주차 등장 확정
5. ~~조우와 성적 귀속~~ **확정(2026-08-11)**: 카드 성적 귀속은 선행 조우가 있어도
   **미니게임 결과**가 맡는다. 현행 `RESOLVE_ENCOUNTER`의 outcome → 성공/실패 정산은
   세션 ②(조우 → 미니게임 체인)에서 미니게임 귀속으로 대체할 것
6. ~~주간 마감 흐름~~ **확정(2026-08-11) · 구현 완료**: 금요일 일과 종료 → 주간 총평 **장면**(상사/본부 구두,
   통지서는 이 자리에서 발행) → 주말 2일(택2 활동) → 엔딩. 엔딩 진입은 주말 뒤로 이동됨
   (weekTally 합산은 금요일 시점 값 유지 — 테스트로 고정).
   **1차 플레이테스트 반영(2026-08-11)**: 해고는 총평 장면 없이 곧장 엔딩(주말 없음) /
   ENC-001은 설명형 조우(briefing) / 빈 저녁은 event 단계 생략 / 같은 지점 마커는 부챗꼴 배치

---

## 7. 판정 통과 후

- 4주 사이클 확장 (승진·해고 판정식) — 1주차 엔딩(content-grid-week1 §D)을 사이클 판정으로 대체
- 다일 이벤트를 카드로 흡수할지 결정 (긴 카드 = 여러 날 점유)
- 미니게임 난이도와 노후도의 결합식

### 별도 라인 (위 결정과 무관, 1주차 판정 통과 후 유지)

- 지도 이미지 생성
- 아이콘 선정·등록부 (design-structure §2-5 아이콘 문법 3원칙은 후보군을 미리 자르는 용도로만 선행 적용)
- SVGR 파이프라인
- 지도 UI

---

## 8. 규칙

**판정 게이트에 도달한 코드베이스에는 기능을 추가하지 않는다.**
현재 상태가 이미 계획 범위를 초과했으므로 추가는 역효과이며,
판정 실패 시 원인 귀속이 불가능해진다.
